// HOC che farà da wrapper per il mio layout
// di base ritorna tutto ciò che sarà all'interno di questo componente (attravero props.children)

const aux = ( props ) => props.children;

export default aux;